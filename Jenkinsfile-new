pipeline {
    // add your slave label name
    agent { label 'slave'}
    tools{
        maven 'maven-ssh'
    }
    stages {
        stage ('Checkout_SCM') {

            steps {
          	    
	     checkout scm
            }
        }

        stage ('Maven_Build') {

            steps {
               sh 'mvn clean package'
            }
        }
        
        stage ('Deploy_Tomcat') {

            steps {
               sshagent(['ssh-web']) {
              sh "scp -o StrictHostKeyChecking=no  target/maven-web-application.war  ec2-user@51.20.86.235:/opt/tomcat9/webapps"
            
                    
            }
         }
        }
        
    }
}
