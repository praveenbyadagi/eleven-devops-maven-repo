pipeline {
    // add your slave label name
    agent { label 'my_maven_slave2'}
    tools{
        maven 'praveen'
    }
    stages {
        stage ('Checkout_SCM') {

            steps {
          	    
	     checkout scm
            }
        }

        stage ('Maven_Build') {

            steps {
               sh 'mvn clean package'
            }
        }
        
        stage ('Deploy_Tomcat') {

            steps {
               sshagent(['tomcat-web-server']) {
              sh "scp -o StrictHostKeyChecking=no  target/maven-web-application.war  ec2-user@51.20.86.235:/opt/tomcat9/webapps"
            
                    
            }
         }
        }
        
    }
}
